_target_: vio_datamodule.VIODataModule
batch_size: 32 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 4
pin_memory: False
train_loader:
  _target_: latent_kitti_dataset.LatentVectorDataset
  _partial_: false
  root_dir: ${paths.data_dir}/kitti_latent_data/train_11
val_loader:
  _target_: latent_kitti_dataset.LatentVectorDataset
  _partial_: false
  root_dir: ${paths.data_dir}/kitti_latent_data/val_11
test_loader: 
  _target_: latent_kitti_dataset.LatentVectorDataset
  _partial_: false
  root_dir: ${paths.data_dir}/kitti_latent_data/val_11
